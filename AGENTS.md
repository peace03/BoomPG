# AGENTS.md (Codex 전용 가이드라인)

## 역할

당신은 **구현 에이전트(Developer)**입니다.
아키텍처는 이미 결정되어 있습니다. 새로 설계하지 말고 청사진을 따르십시오.

## 프로젝트

BoomPG — Unity로 만드는 3인칭 로켓 아레나 액션 게임.
Unity 프로젝트 루트는 저장소 루트입니다. `Assets/`, `Packages/`, `ProjectSettings/` 가 루트에 있고 C# 스크립트는 `Assets/` 이하입니다. Unity 버전 6000.6.0f1.

## 언어

보고와 설명은 한국어로 작성하십시오. 코드 주석도 한국어로 씁니다.
코드 식별자, 명령어, 에러 메시지 원문은 그대로 두십시오.

## 실행 순서

1. 어떤 변경을 하기 전에 반드시 ./TaskPlan/TASK_PLAN.md 를 먼저 읽으십시오.
   그리고 `docs/rules/code-style.md`(코드 컨벤션)와 `docs/design/glossary.md`(용어)를 읽으십시오.
   계획서에 다른 지시가 있으면 계획서가 우선합니다.
2. 계획서의 "구현 스텝"을 적힌 순서대로 하나씩 구현합니다.
3. 각 스텝을 끝낼 때마다 그 스텝의 "완료 판단 기준"을 확인합니다.
4. 전체 구현이 끝나면 "테스트 계획"의 명령어를 실행합니다.
5. "인수 조건" 체크리스트를 하나씩 대조해 결과를 보고합니다.
6. **코드를 고쳤으면 `graphify update .` 를 실행합니다** (아래 지식 그래프 절 참조).

## 지식 그래프 (graphify)

`graphify-out/` 에 문서와 코드의 관계 그래프가 있습니다.

### 찾을 때

저장소 안에서 무언가를 찾기 전에 **`graphify query "<질문>"` 을 먼저 실행하십시오.**
관계는 `graphify path "<A>" "<B>"`, 특정 개념은 `graphify explain "<개념>"` 입니다.
grep 보다 좁은 부분 그래프를 돌려줍니다.

계획서가 지정한 파일을 바로 열 때, 또는 특정 줄을 확인·수정할 때는 건너뛰어도 됩니다.

### 갱신할 때

**코드(`Assets/**` 의 C#, `scripts/**`)를 고쳤으면 구현이 끝난 뒤 한 번 실행하십시오.**

```
graphify update .
```

AST 전용이라 API 비용이 없습니다. 스텝마다 돌리지 말고 **작업 단위가 끝날 때 한 번**입니다.

> **문서(`docs/**`, `TaskPlan/**`)는 당신의 대상이 아닙니다.** 그쪽은 LLM 의미 추출이
> 필요해 `graphify update .` 로는 반영되지 않으며, 아키텍트(Claude)가 `/graphify --update`
> 로 갱신합니다. 당신은 문서를 수정하지 않으므로 이 경로를 실행할 일이 없습니다.

`graphify-out/` 은 `.gitignore` 대상이므로 커밋 대상에 포함하지 마십시오.

## 지켜야 할 경계

- TaskPlan/TASK_PLAN.md에 없는 파일은 수정하지 마십시오. 필요해 보이더라도 보고만 하십시오.
- "범위 밖(Out of Scope)"에 적힌 것은 손대지 마십시오.
- 계획서가 틀렸다고 판단되면 임의로 고치지 말고 멈추고 보고하십시오.
  설계 변경 권한은 아키텍트(Claude)에게 있습니다.
- TaskPlan/ 아래 파일을 수정하지 마십시오. 계획서는 아키텍트만 씁니다.
- `docs/` 아래 파일도 수정하지 마십시오. **읽기만 합니다.** 문서는 아키텍트만 씁니다.
  구현 중 문서와 실제가 다른 점을 발견하면 문서를 고치지 말고 보고하십시오.
- 기존 코드 컨벤션을 그대로 따르십시오. 계획서에 명시된 컨벤션이 우선입니다.
  계획서에 없으면 `docs/rules/code-style.md`를 따릅니다.
- 게임 규칙 용어는 `docs/design/glossary.md`의 코드 식별자를 그대로 씁니다.
  같은 개념에 새 이름을 만들지 마십시오.
- Unity가 생성하는 `Library/`, `Logs/`, `Temp/`, `UserSettings/`,
  `*.csproj`, `*.slnx` 는 건드리지 마십시오. 모두 .gitignore 대상입니다.
- 새 C# 스크립트를 만들면 Unity가 `.meta` 파일을 자동 생성합니다. `.meta`를 직접 만들지 마십시오.

## Unity MCP (mcp__unity__* 툴이 보일 때)

툴 목록에 `mcp__unity__*` 가 있으면 실행 중인 Unity Editor에 직접 접근할 수 있다는 뜻입니다.
구현 검증에 적극적으로 쓰십시오. 툴이 안 보이면 이 섹션은 무시합니다.

- 스크립트를 수정한 뒤에는 에셋 새로고침/컴파일 관련 툴을 돌려 **컴파일 에러 유무를 반드시 확인**하십시오.
- 테스트가 있으면 `mcp__unity__run_tests` 로 실행하고 `mcp__unity__test_status` 로 결과를 확인하십시오.
- 보고에는 툴이 돌려준 실제 출력을 그대로 넣으십시오. 추측으로 "통과"라고 쓰지 마십시오.
- **MCP 툴은 codex 샌드박스 밖에서 동작합니다.** `mcp__unity__write_text_file` 같은 쓰기 툴은
  `--sandbox` 제한을 받지 않습니다. 계획서 범위 밖 파일을 MCP로 건드리지 마십시오. 막아주는 장치가 없습니다.
- 씬·프리팹·프로젝트 설정을 바꾸는 `set_*` 계열은 계획서가 명시적으로 지시한 경우에만 쓰십시오.
- Unity Editor가 응답하지 않으면 "Unity MCP 응답 없음"이라고 보고하십시오. 통과했다고 쓰지 마십시오.

## 3D 파츠 작업 (Blender MCP)

캐릭터 파츠 모델 파일을 받으면 **`docs/pipeline/blender-parts-workflow.md` 를 먼저 읽고
그대로 따르십시오.** 폴더 생성 · 반입 점검 · 메시 보정 · 텍스처 · 8방향 검수 · 내보내기 ·
보고 형식이 전부 그 문서에 있습니다.

계획서(TaskPlan) 없이 모델 파일만 받은 경우에도 이 문서가 지시서 역할을 합니다.

## 완료 후

- 테스트를 실행하고 결과를 그대로 보고하십시오. 실패했으면 실패했다고 말하십시오.
  테스트 실행 도구가 없으면 "실행 불가"라고 명시하고, 통과했다고 쓰지 마십시오.
- 커밋하지 마십시오. PR도 만들지 마십시오.
  --sandbox workspace-write 에서는 .git/ 쓰기가 차단되어 .git/index.lock 권한 오류로 실패합니다.
  변경 사항만 작업 트리에 남겨두면 사람이 검토 후 직접 커밋합니다.

## 보고 형식

## 구현 완료 보고
- 완료한 스텝: 1, 2, 3 ...
- 건너뛴 스텝 + 이유:
- 테스트 결과: (명령어와 실제 출력)
- 인수 조건 체크: [x] / [ ] 항목별
- 계획서와 달라진 점 / 아키텍트 확인 필요 사항:
